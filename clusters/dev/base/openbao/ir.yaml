apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openbao
  namespace: openbao
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`openbao.lappis.rocks`)
      kind: Rule
      services:
        - name: openbao
          port: 8200
